{
  "devDependencies": {
    "husky": "^9.0.10",
    "lint-staged": "^16.0.0",
    "prettier": "^3.2.5"
  },
  "lint-staged": {
    "**/*.php": "./vendor/bin/pint --dirty",
    "**/*.json": "prettier",
    "**/*.md": "prettier"
  },
  "scripts": {
    "prepare": "husky"
  }
}
